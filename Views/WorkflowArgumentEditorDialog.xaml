<Window x:Class="ActiproRoslynPOC.Views.WorkflowArgumentEditorDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:ActiproRoslynPOC.Views"
        Title="工作流参数编辑器"
        Height="550"
        Width="900"
        WindowStartupLocation="CenterOwner"
        ResizeMode="CanResize">
    <Window.Resources>
        <!-- Direction 枚举值 -->
        <ObjectDataProvider x:Key="DirectionValues"
                            MethodName="GetValues"
                            ObjectType="{x:Type local:WorkflowWorkflowArgumentDirection}">
            <ObjectDataProvider.MethodParameters>
                <x:Type TypeName="local:WorkflowWorkflowArgumentDirection"/>
            </ObjectDataProvider.MethodParameters>
        </ObjectDataProvider>

        <!-- 常用类型列表 -->
        <x:Array x:Key="CommonTypes" Type="{x:Type sys:String}" xmlns:sys="clr-namespace:System;assembly=mscorlib">
            <sys:String>String</sys:String>
            <sys:String>Int32</sys:String>
            <sys:String>Int64</sys:String>
            <sys:String>Double</sys:String>
            <sys:String>Boolean</sys:String>
            <sys:String>DateTime</sys:String>
            <sys:String>Object</sys:String>
            <sys:String>String[]</sys:String>
            <sys:String>List&lt;String&gt;</sys:String>
            <sys:String>Dictionary&lt;String, Object&gt;</sys:String>
        </x:Array>
    </Window.Resources>

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- 工作流文件路径 -->
        <GroupBox Grid.Row="0" Header="工作流信息" Margin="0,0,0,10">
            <Grid Margin="5">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Row="0" Grid.Column="0"
                           Text="文件路径:"
                           VerticalAlignment="Center"
                           Margin="0,0,10,5"/>
                <TextBox Grid.Row="0" Grid.Column="1"
                         Name="WorkflowPathTextBox"
                         VerticalAlignment="Center"
                         Margin="0,0,5,5"
                         IsReadOnly="False"
                         Background="White"
                         TextChanged="OnWorkflowPathChanged"/>
                <Button Grid.Row="0" Grid.Column="2"
                        Content="分析参数"
                        Width="80"
                        Height="26"
                        Margin="0,0,0,5"
                        Click="OnAnalyzeClick"/>

                <TextBlock Grid.Row="1" Grid.Column="0"
                           Text="工作流类:"
                           VerticalAlignment="Center"
                           Margin="0,0,10,0"/>
                <TextBox Grid.Row="1" Grid.Column="1" Grid.ColumnSpan="2"
                         Name="WorkflowClassTextBox"
                         VerticalAlignment="Center"
                         IsReadOnly="True"
                         Background="#F5F5F5"/>
            </Grid>
        </GroupBox>

        <!-- 说明文字 -->
        <TextBlock Grid.Row="1"
                   Text="编辑工作流参数。In 参数需要提供值，Out 参数需要指定接收变量。Value 支持 VB 表达式。"
                   Margin="0,0,0,10"
                   TextWrapping="Wrap"
                   Foreground="#666"/>

        <!-- 参数列表 (类似 UiPath 风格) -->
        <DataGrid Grid.Row="2"
                  Name="ArgumentsDataGrid"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  CanUserDeleteRows="False"
                  CanUserReorderColumns="False"
                  SelectionMode="Single"
                  GridLinesVisibility="All"
                  AlternatingRowBackground="#F9F9F9"
                  RowHeight="32"
                  HeadersVisibility="Column"
                  BorderBrush="#DDD"
                  BorderThickness="1">
            <DataGrid.ColumnHeaderStyle>
                <Style TargetType="DataGridColumnHeader">
                    <Setter Property="Background" Value="#F5F5F5"/>
                    <Setter Property="Padding" Value="8,5"/>
                    <Setter Property="FontWeight" Value="SemiBold"/>
                    <Setter Property="BorderBrush" Value="#DDD"/>
                    <Setter Property="BorderThickness" Value="0,0,1,1"/>
                </Style>
            </DataGrid.ColumnHeaderStyle>

            <DataGrid.Columns>
                <!-- Key 列 -->
                <DataGridTextColumn Header="Key"
                                    Binding="{Binding Name, UpdateSourceTrigger=PropertyChanged}"
                                    Width="150"
                                    MinWidth="100">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="Padding" Value="8,0"/>
                            <Setter Property="VerticalAlignment" Value="Center"/>
                            <Setter Property="FontWeight" Value="SemiBold"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                    <DataGridTextColumn.EditingElementStyle>
                        <Style TargetType="TextBox">
                            <Setter Property="Padding" Value="5,0"/>
                        </Style>
                    </DataGridTextColumn.EditingElementStyle>
                </DataGridTextColumn>

                <!-- Type 列 (下拉选择) -->
                <DataGridComboBoxColumn Header="Type"
                                        SelectedItemBinding="{Binding TypeName, UpdateSourceTrigger=PropertyChanged}"
                                        ItemsSource="{StaticResource CommonTypes}"
                                        Width="150">
                    <DataGridComboBoxColumn.ElementStyle>
                        <Style TargetType="ComboBox">
                            <Setter Property="IsEditable" Value="True"/>
                        </Style>
                    </DataGridComboBoxColumn.ElementStyle>
                    <DataGridComboBoxColumn.EditingElementStyle>
                        <Style TargetType="ComboBox">
                            <Setter Property="IsEditable" Value="True"/>
                        </Style>
                    </DataGridComboBoxColumn.EditingElementStyle>
                </DataGridComboBoxColumn>

                <!-- Direction 列 (下拉选择) -->
                <DataGridComboBoxColumn Header="Direction"
                                        SelectedItemBinding="{Binding Direction, UpdateSourceTrigger=PropertyChanged}"
                                        ItemsSource="{Binding Source={StaticResource DirectionValues}}"
                                        Width="80">
                </DataGridComboBoxColumn>

                <!-- Value 列 -->
                <DataGridTemplateColumn Header="Value" Width="*" MinWidth="200">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Grid.Column="0"
                                           Text="{}"
                                           Foreground="#999"
                                           VerticalAlignment="Center"
                                           Margin="8,0,4,0"/>
                                <TextBlock Grid.Column="1"
                                           Text="{Binding Value}"
                                           VerticalAlignment="Center"
                                           Foreground="#666"
                                           TextTrimming="CharacterEllipsis">
                                    <TextBlock.Style>
                                        <Style TargetType="TextBlock">
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding Value}" Value="">
                                                    <Setter Property="Text" Value="Enter a VB expression"/>
                                                    <Setter Property="Foreground" Value="#BBB"/>
                                                    <Setter Property="FontStyle" Value="Italic"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding Value}" Value="{x:Null}">
                                                    <Setter Property="Text" Value="Enter a VB expression"/>
                                                    <Setter Property="Foreground" Value="#BBB"/>
                                                    <Setter Property="FontStyle" Value="Italic"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBlock.Style>
                                </TextBlock>
                            </Grid>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                    <DataGridTemplateColumn.CellEditingTemplate>
                        <DataTemplate>
                            <TextBox Text="{Binding Value, UpdateSourceTrigger=PropertyChanged}"
                                     Padding="5,0"
                                     BorderThickness="0"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellEditingTemplate>
                </DataGridTemplateColumn>

                <!-- 操作按钮列 -->
                <DataGridTemplateColumn Width="70">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                <Button Content="+" Width="24" Height="24" Margin="2,0"
                                        Click="OnAddArgumentClick"
                                        ToolTip="添加参数"
                                        FontWeight="Bold"/>
                                <Button Content="🗑" Width="24" Height="24" Margin="2,0"
                                        Click="OnDeleteArgumentClick"
                                        ToolTip="删除参数"/>
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>

        <!-- 添加参数按钮 -->
        <StackPanel Grid.Row="3" Orientation="Horizontal" Margin="0,10,0,0">
            <Button Content="+ 添加参数"
                    Width="100"
                    Height="28"
                    Click="OnAddNewArgumentClick"/>
        </StackPanel>

        <!-- 按钮栏 -->
        <StackPanel Grid.Row="4"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    Margin="0,10,0,0">
            <Button Content="确定"
                    Width="80"
                    Height="32"
                    Margin="0,0,10,0"
                    Click="OnOkClick"
                    IsDefault="True"/>
            <Button Content="取消"
                    Width="80"
                    Height="32"
                    Click="OnCancelClick"
                    IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>
