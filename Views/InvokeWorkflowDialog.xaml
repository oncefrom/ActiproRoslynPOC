<Window x:Class="ActiproRoslynPOC.Views.InvokeWorkflowDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="调用工作流"
        Height="500" Width="600"
        WindowStartupLocation="CenterOwner"
        ResizeMode="CanResizeWithGrip">

    <Window.Resources>
        <Style x:Key="LabelStyle" TargetType="TextBlock">
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Margin" Value="0,0,10,0"/>
        </Style>
        <Style x:Key="InputStyle" TargetType="TextBox">
            <Setter Property="Margin" Value="0,5"/>
            <Setter Property="Padding" Value="5,3"/>
        </Style>
    </Window.Resources>

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- 文件选择区域 -->
        <GroupBox Grid.Row="0" Header="选择工作流文件" Margin="0,0,0,10">
            <Grid Margin="10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <ComboBox x:Name="cmbWorkflowFiles"
                          Grid.Column="0"
                          Margin="0,0,10,0"
                          DisplayMemberPath="DisplayName"
                          SelectionChanged="OnWorkflowFileSelectionChanged"/>

                <Button x:Name="btnBrowse"
                        Grid.Column="1"
                        Content="浏览..."
                        Padding="15,5"
                        Click="OnBrowseClick"/>
            </Grid>
        </GroupBox>

        <!-- 工作流信息 -->
        <GroupBox Grid.Row="1" Header="工作流信息" Margin="0,0,0,10">
            <Grid Margin="10">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="80"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Row="0" Grid.Column="0" Text="名称:" Style="{StaticResource LabelStyle}"/>
                <TextBlock x:Name="txtWorkflowName" Grid.Row="0" Grid.Column="1"
                           VerticalAlignment="Center" FontWeight="Bold"/>

                <TextBlock Grid.Row="1" Grid.Column="0" Text="类名:" Style="{StaticResource LabelStyle}" Margin="0,5,10,0"/>
                <TextBlock x:Name="txtClassName" Grid.Row="1" Grid.Column="1"
                           VerticalAlignment="Center" Margin="0,5,0,0"/>

                <TextBlock Grid.Row="2" Grid.Column="0" Text="返回类型:" Style="{StaticResource LabelStyle}" Margin="0,5,10,0"/>
                <TextBlock x:Name="txtReturnType" Grid.Row="2" Grid.Column="1"
                           VerticalAlignment="Center" Foreground="#267F99" Margin="0,5,0,0"/>
            </Grid>
        </GroupBox>

        <!-- 参数输入区域 -->
        <GroupBox Grid.Row="2" Header="输入参数" Margin="0,0,0,10">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <ItemsControl x:Name="parametersPanel" Margin="10">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Grid Margin="0,5">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="120"/>
                                    <ColumnDefinition Width="80"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>

                                <!-- 参数名 -->
                                <TextBlock Grid.Column="0"
                                           Text="{Binding Name}"
                                           VerticalAlignment="Center"
                                           FontWeight="SemiBold"/>

                                <!-- 类型 -->
                                <TextBlock Grid.Column="1"
                                           Text="{Binding DisplayTypeName}"
                                           VerticalAlignment="Center"
                                           Foreground="#267F99"
                                           Margin="5,0"/>

                                <!-- 输入框 -->
                                <TextBox Grid.Column="2"
                                         Text="{Binding Value, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                         Style="{StaticResource InputStyle}"
                                         ToolTip="{Binding TooltipText}"/>
                            </Grid>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </GroupBox>

        <!-- 无参数提示 -->
        <TextBlock x:Name="txtNoParameters"
                   Grid.Row="2"
                   Text="该工作流没有输入参数"
                   HorizontalAlignment="Center"
                   VerticalAlignment="Center"
                   Foreground="Gray"
                   Visibility="Collapsed"/>

        <!-- 输出参数映射（可选） -->
        <GroupBox x:Name="grpOutputMapping" Grid.Row="3" Header="输出参数映射（可选）" Margin="0,0,0,10" Visibility="Collapsed">
            <Grid Margin="10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="80"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0" Text="保存到:" Style="{StaticResource LabelStyle}"/>
                <TextBox x:Name="txtOutputVariable" Grid.Column="1"
                         Style="{StaticResource InputStyle}"
                         ToolTip="输入变量名以保存工作流返回值"/>
            </Grid>
        </GroupBox>

        <!-- 按钮区域 -->
        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button x:Name="btnOK" Content="确定"
                    Width="80" Margin="0,0,10,0"
                    IsDefault="True"
                    Click="OnOKClick"/>
            <Button x:Name="btnCancel" Content="取消"
                    Width="80"
                    IsCancel="True"
                    Click="OnCancelClick"/>
        </StackPanel>
    </Grid>
</Window>
