<sap:ActivityDesigner x:Class="ActiproRoslynPOC.Activities.InvokeWorkflowDesigner"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:sap="clr-namespace:System.Activities.Presentation;assembly=System.Activities.Presentation"
    xmlns:sapv="clr-namespace:System.Activities.Presentation.View;assembly=System.Activities.Presentation"
    xmlns:sapc="clr-namespace:System.Activities.Presentation.Converters;assembly=System.Activities.Presentation"
    xmlns:scg="clr-namespace:System.Collections.Generic;assembly=mscorlib"
    xmlns:local="clr-namespace:ActiproRoslynPOC.Activities">

    <sap:ActivityDesigner.Resources>
        <sapc:ArgumentToExpressionConverter x:Key="ArgumentToExpressionConverter" />
    </sap:ActivityDesigner.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>

        <!-- 标题栏 -->
        <Border Grid.Row="0" Grid.ColumnSpan="3"
                Background="#FF2196F3"
                BorderBrush="#FF1976D2"
                BorderThickness="1"
                CornerRadius="3,3,0,0"
                Padding="8,4">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="📋" FontSize="14" Margin="0,0,5,0" VerticalAlignment="Center"/>
                <TextBlock Text="调用 XAML 工作流"
                          Foreground="White"
                          FontWeight="Bold"
                          VerticalAlignment="Center"/>
            </StackPanel>
        </Border>

        <!-- WorkflowFilePath 标签 -->
        <TextBlock Grid.Row="1" Grid.Column="0"
                   Text="工作流文件:"
                   Margin="8,8,8,4"
                   VerticalAlignment="Center"
                   FontWeight="SemiBold"/>

        <!-- WorkflowFilePath 编辑器 -->
        <sapv:ExpressionTextBox Grid.Row="1" Grid.Column="1"
                                Margin="0,8,8,4"
                                Height="24"
                                HintText="工作流文件路径 (如: Workflows/SubWorkflow.xaml)"
                                OwnerActivity="{Binding Path=ModelItem}"
                                Expression="{Binding Path=ModelItem.WorkflowFilePath, Mode=TwoWay, Converter={StaticResource ArgumentToExpressionConverter}, ConverterParameter=In}"
                                ExpressionType="s:String"
                                UseLocationExpression="False"
                                MaxLines="1"
                                xmlns:s="clr-namespace:System;assembly=mscorlib"/>

        <!-- Arguments 标签 -->
        <TextBlock Grid.Row="2" Grid.Column="0"
                   Text="参数字典:"
                   Margin="8,4,8,8"
                   VerticalAlignment="Top"
                   FontWeight="SemiBold"/>

        <!-- Arguments 编辑器 (多行) -->
        <sapv:ExpressionTextBox Grid.Row="2" Grid.Column="1"
                                Margin="0,4,4,8"
                                MinHeight="60"
                                HintText="New Dictionary(Of String, Object) From {{&quot;key1&quot;, &quot;value1&quot;}, {&quot;key2&quot;, 123}}"
                                OwnerActivity="{Binding Path=ModelItem}"
                                Expression="{Binding Path=ModelItem.Arguments, Mode=TwoWay, Converter={StaticResource ArgumentToExpressionConverter}, ConverterParameter=In}"
                                UseLocationExpression="False"
                                VerticalScrollBarVisibility="Auto"/>

        <!-- Arguments 编辑按钮 -->
        <Button Grid.Row="2" Grid.Column="2"
                Content="..."
                Width="30"
                Height="30"
                Margin="0,4,8,8"
                VerticalAlignment="Top"
                Click="OnEditArgumentsClick"
                ToolTip="打开参数编辑器"/>
    </Grid>
</sap:ActivityDesigner>
