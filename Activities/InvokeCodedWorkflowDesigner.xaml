<sap:ActivityDesigner x:Class="ActiproRoslynPOC.Activities.InvokeCodedWorkflowDesigner"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:sap="clr-namespace:System.Activities.Presentation;assembly=System.Activities.Presentation"
    xmlns:sapv="clr-namespace:System.Activities.Presentation.View;assembly=System.Activities.Presentation"
    xmlns:sapc="clr-namespace:System.Activities.Presentation.Converters;assembly=System.Activities.Presentation"
    xmlns:scg="clr-namespace:System.Collections.Generic;assembly=mscorlib"
    xmlns:local="clr-namespace:ActiproRoslynPOC.Activities">

    <sap:ActivityDesigner.Resources>
        <sapc:ArgumentToExpressionConverter x:Key="ArgumentToExpressionConverter" />
    </sap:ActivityDesigner.Resources>

    <Grid MinWidth="350">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>

        <!-- 标题栏 -->
        <Border Grid.Row="0" Grid.ColumnSpan="3"
                Background="#FF4CAF50"
                BorderBrush="#FF388E3C"
                BorderThickness="1"
                CornerRadius="3,3,0,0"
                Padding="8,4">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="📄" FontSize="14" Margin="0,0,5,0" VerticalAlignment="Center"/>
                <TextBlock Text="调用 C# 工作流"
                          Foreground="White"
                          FontWeight="Bold"
                          VerticalAlignment="Center"/>
            </StackPanel>
        </Border>

        <!-- WorkflowFilePath 标签 -->
        <TextBlock Grid.Row="1" Grid.Column="0"
                   Text="工作流文件:"
                   Margin="8,8,8,4"
                   VerticalAlignment="Center"
                   FontWeight="SemiBold"/>

        <!-- WorkflowFilePath 编辑器 -->
        <sapv:ExpressionTextBox Grid.Row="1" Grid.Column="1" Grid.ColumnSpan="2"
                                Margin="0,8,8,4"
                                Height="24"
                                HintText="工作流文件路径 (如: Workflows/MyWorkflow.cs)"
                                OwnerActivity="{Binding Path=ModelItem}"
                                Expression="{Binding Path=ModelItem.WorkflowFilePath, Mode=TwoWay, Converter={StaticResource ArgumentToExpressionConverter}, ConverterParameter=In}"
                                ExpressionType="s:String"
                                UseLocationExpression="False"
                                MaxLines="1"
                                xmlns:s="clr-namespace:System;assembly=mscorlib"/>

        <!-- 编辑参数按钮 -->
        <Button Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="3"
                Content="编辑参数..."
                Height="28"
                Margin="8,4,8,4"
                HorizontalAlignment="Stretch"
                Click="OnEditArgumentsClick"
                ToolTip="打开参数编辑器（类似 UiPath 风格）"/>

        <!-- 参数预览区域 -->
        <Border Grid.Row="3" Grid.Column="0" Grid.ColumnSpan="3"
                Margin="8,4,8,4"
                BorderBrush="#DDD"
                BorderThickness="1"
                Background="#FAFAFA"
                CornerRadius="3"
                Padding="8"
                Visibility="{Binding ElementName=ArgumentsPreviewTextBlock, Path=Text, Converter={StaticResource ArgumentToExpressionConverter}}">
            <StackPanel>
                <TextBlock Text="参数预览:" FontWeight="SemiBold" Foreground="#666" Margin="0,0,0,4"/>
                <TextBlock Name="ArgumentsPreviewTextBlock"
                           Text="(点击上方按钮编辑参数)"
                           Foreground="#999"
                           FontSize="11"
                           TextWrapping="Wrap"
                           MaxHeight="80"/>
            </StackPanel>
        </Border>

        <!-- Result 输出 -->
        <TextBlock Grid.Row="4" Grid.Column="0"
                   Text="Result:"
                   Margin="8,4,8,8"
                   VerticalAlignment="Center"
                   FontWeight="SemiBold"/>

        <sapv:ExpressionTextBox Grid.Row="4" Grid.Column="1" Grid.ColumnSpan="2"
                                Margin="0,4,8,8"
                                Height="24"
                                HintText="接收输出结果的变量"
                                OwnerActivity="{Binding Path=ModelItem}"
                                Expression="{Binding Path=ModelItem.Result, Mode=TwoWay, Converter={StaticResource ArgumentToExpressionConverter}, ConverterParameter=Out}"
                                ExpressionType="s:Object"
                                UseLocationExpression="True"
                                xmlns:s="clr-namespace:System;assembly=mscorlib"/>
    </Grid>
</sap:ActivityDesigner>
