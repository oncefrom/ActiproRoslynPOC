<Window x:Class="ActiproRoslynPOC.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:themes="http://schemas.actiprosoftware.com/winfx/xaml/themes"
        xmlns:editor="http://schemas.actiprosoftware.com/winfx/xaml/syntaxeditor"
        xmlns:docking="http://schemas.actiprosoftware.com/winfx/xaml/docking"
        Title="{Binding WindowTitle}" Width="1400" Height="900">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Menu Grid.Row="0" />

        <ToolBarTray Grid.Row="1">
            <ToolBar>
                <Button Command="{Binding NewFileCommand}">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="✚" Foreground="White" Margin="0,0,5,0"/>
                        <TextBlock Text="新建工作流"/>
                    </StackPanel>
                </Button>
                <Separator/>
                <Button Command="{Binding RunCommand}">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="▶" Foreground="#4EC9B0" Margin="0,0,5,0"/>
                        <TextBlock Text="运行工作流"/>
                    </StackPanel>
                </Button>
                <Separator/>
                <Button Command="{Binding SaveCommand}">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="💾" Margin="0,0,5,0"/>
                        <TextBlock Text="保存"/>
                    </StackPanel>
                </Button>
                <Separator/>
                <Button Click="OnToggleThemeClick">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="🎨" Margin="0,0,5,0"/>
                        <TextBlock x:Name="themeButtonText" Text="切换深色主题"/>
                    </StackPanel>
                </Button>
                <Separator/>
                <Button Click="OnSettingsClick" ToolTip="编辑器设置">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="⚙" Margin="0,0,5,0"/>
                        <TextBlock Text="设置"/>
                    </StackPanel>
                </Button>
            </ToolBar>

            <!-- 调试工具栏 -->
            <ToolBar>
                <TextBlock Text="调试:" VerticalAlignment="Center" Margin="5,0"/>
                <Separator/>
                <Button Command="{Binding StartDebugCommand}" ToolTip="开始调试 (F9)">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="🐛" Margin="0,0,5,0"/>
                        <TextBlock Text="开始"/>
                    </StackPanel>
                </Button>
                <Button Command="{Binding StepOverCommand}" ToolTip="单步执行 (F10)">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="⏭" Margin="0,0,5,0"/>
                        <TextBlock Text="单步"/>
                    </StackPanel>
                </Button>
                <Button Command="{Binding ContinueCommand}" ToolTip="继续执行 (F5)">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="▶" Foreground="Green" Margin="0,0,5,0"/>
                        <TextBlock Text="继续"/>
                    </StackPanel>
                </Button>
                <Button Command="{Binding StopDebugCommand}" ToolTip="停止调试 (Shift+F5)">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="⏹" Foreground="Red" Margin="0,0,5,0"/>
                        <TextBlock Text="停止"/>
                    </StackPanel>
                </Button>
            </ToolBar>
        </ToolBarTray>

        <!-- Actipro DockSite 多文档布局 -->
        <docking:DockSite x:Name="dockSite" Grid.Row="2"
                          DocumentItemsSource="{Binding OpenDocuments}"
                          WindowsClosing="dockSite_WindowsClosing"
                          WindowActivated="DockSite_WindowActivated">
            <docking:SplitContainer Orientation="Vertical">

                <docking:Workspace>
                    <docking:TabbedMdiHost x:Name="tabbedMdiHost" />
                </docking:Workspace>

                <docking:ToolWindowContainer>
                    <docking:ToolWindow Title="输出" IsOpen="True">
                        <TextBox Text="{Binding Output}"
         IsReadOnly="True"
         BorderThickness="0"
         FontFamily="Consolas"
         VerticalScrollBarVisibility="Auto"/>
                    </docking:ToolWindow>
                    <docking:ToolWindow Title="错误列表" IsOpen="True">
                        <DataGrid ItemsSource="{Binding Diagnostics}"
          AutoGenerateColumns="False"
          BorderThickness="0">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="行" Binding="{Binding Line}" Width="50"/>
                                <DataGridTextColumn Header="描述" Binding="{Binding Message}" Width="*"/>
                            </DataGrid.Columns>
                        </DataGrid>
                    </docking:ToolWindow>
                    <docking:ToolWindow Title="变量" IsOpen="True">
                        <TextBox Text="{Binding VariablesText}"
         IsReadOnly="True"
         BorderThickness="0"
         FontFamily="Consolas"
         VerticalScrollBarVisibility="Auto"/>
                    </docking:ToolWindow>
                </docking:ToolWindowContainer>
            </docking:SplitContainer>
            <docking:DockSite.AutoHideLeftContainers>
                <docking:ToolWindowContainer>
                    <docking:ToolWindow Title="项目文件" IsOpen="True">
                        <ListBox x:Name="fileListBox"
         BorderThickness="0"
         MouseDoubleClick="OnFileListBoxMouseDoubleClick">
                            <ListBox.ItemTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="📄" Margin="0,0,5,0"/>
                                        <TextBlock Text="{Binding}"/>
                                    </StackPanel>
                                </DataTemplate>
                            </ListBox.ItemTemplate>
                        </ListBox>
                    </docking:ToolWindow>
                </docking:ToolWindowContainer>
            </docking:DockSite.AutoHideLeftContainers>
            
        </docking:DockSite>

        <StatusBar Grid.Row="3">
            <StatusBarItem Content="Ready"/>
            <StatusBarItem>
                <TextBlock x:Name="debugStatusText" Foreground="#4EC9B0" FontWeight="Bold"/>
            </StatusBarItem>
            <StatusBarItem HorizontalAlignment="Right">
                <StackPanel Orientation="Horizontal">
                    <TextBlock x:Name="linePanel" Text="Ln 1" Margin="10,0"/>
                    <TextBlock x:Name="columnPanel" Text="Col 1" Margin="10,0"/>
                </StackPanel>
            </StatusBarItem>
        </StatusBar>
    </Grid>
</Window>
